apiVersion: v1
kind: Service
metadata:
  name: vulnerable-app-service
  namespace: vulnerable-demo
  labels:
    app: vulnerable-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: vulnerable-app
